# Anthropic Claude API key — for the Ask feature's language model
# Get one at https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key — used only to generate embeddings (one-time, ~$0.02)
# Get one at https://platform.openai.com → API Keys
OPENAI_API_KEY=sk-...
